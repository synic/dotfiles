#!/bin/sh

liquidctl initialize all
liquidctl --match "kraken" set sync color fixed "rgb(255,0,0)"
liquidctl --match "kraken" set ring color breathing "rgb(100,100,100)" "rgb(100,100,100)"
#liquidctl --match "kraken" set logo color fixed 'rgb(255,255,255)'

